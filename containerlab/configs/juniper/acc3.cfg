set system services netconf ssh
set system services ssh root-login allow
set system services ssh

set system host-name acc3

set system login user autonetops class super-user authentication plain-text-password
Netauto@2025

commit